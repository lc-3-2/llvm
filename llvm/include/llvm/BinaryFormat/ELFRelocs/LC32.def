
#ifndef ELF_RELOC
#error "ELF_RELOC must be defined"
#endif

// No relocation
// Not used for anything (by definition), but can be generated by other tools,
// so we have to account for it.
ELF_RELOC(R_LC_3_2_NONE, 0)

// Word relocation
// Substitutes a 32-bit word in little-endian order at the address marked
ELF_RELOC(R_LC_3_2_32, 1)
